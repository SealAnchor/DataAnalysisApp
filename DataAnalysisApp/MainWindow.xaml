<Window x:Class="DataAnalysisApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataAnalysisApp"
        xmlns:vm="clr-namespace:DataAnalysisApp.ViewModels"
        xmlns:converters="clr-namespace:DataAnalysisApp.Converters"
        mc:Ignorable="d"
        Title="CSV数据分析工具" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Icon>
        <DrawingImage>
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <!-- 背景圆形 -->
                    <GeometryDrawing Brush="#2196F3">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="64,64" RadiusX="60" RadiusY="60"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <!-- CSV文件矩形 -->
                    <GeometryDrawing Brush="White">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="20,20,88,104" RadiusX="4" RadiusY="4"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <!-- 文件折角 -->
                    <GeometryDrawing Brush="#BBDEFB">
                        <GeometryDrawing.Geometry>
                            <PathGeometry Figures="M 75,20 L 75,45 L 106,45 Z"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <!-- CSV文字背景 -->
                    <GeometryDrawing Brush="#1976D2">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="30,55,68,20"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <!-- 数据线条 -->
                    <GeometryDrawing Brush="#E0E0E0">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <RectangleGeometry Rect="30,85,68,6" RadiusX="1" RadiusY="1"/>
                                <RectangleGeometry Rect="30,96,68,6" RadiusX="1" RadiusY="1"/>
                                <RectangleGeometry Rect="30,107,68,6" RadiusX="1" RadiusY="1"/>
                                <RectangleGeometry Rect="30,118,48,6" RadiusX="1" RadiusY="1"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <!-- 图表柱状图 -->
                    <GeometryDrawing Brush="#4CAF50">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="102,85,6,40" RadiusX="1" RadiusY="1"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#FF9800">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="112,100,6,25" RadiusX="1" RadiusY="1"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#F44336">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="122,110,6,15" RadiusX="1" RadiusY="1"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
    </Window.Icon>
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:SearchKeywordConverter x:Key="SearchKeywordConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="文件夹:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SelectedFolder}" Width="300" IsReadOnly="True" VerticalAlignment="Center" Padding="5"/>
                <Button Content="浏览..." Command="{Binding BrowseFolderCommand}" Margin="5,0,15,0" Padding="10,5"/>

                <TextBlock Text="查找:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center" Padding="5" KeyDown="SearchTextBox_KeyDown"/>
                <Button Content="搜索" Command="{Binding SearchCommand}" Margin="5,0,15,0" Padding="15,5"/>

                <CheckBox Content="日期筛选" IsChecked="{Binding EnableDateFilter}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="开始:" VerticalAlignment="Center" Margin="0,0,5,0" IsEnabled="{Binding EnableDateFilter}"/>
                <DatePicker SelectedDate="{Binding StartDate}" VerticalAlignment="Center" Margin="0,0,10,0" IsEnabled="{Binding EnableDateFilter}" Width="120"/>
                <TextBlock Text="结束:" VerticalAlignment="Center" Margin="0,0,5,0" IsEnabled="{Binding EnableDateFilter}"/>
                <DatePicker SelectedDate="{Binding EndDate}" VerticalAlignment="Center" IsEnabled="{Binding EnableDateFilter}" Width="120"/>
            </StackPanel>
        </Border>

        <!-- 统计信息 -->
        <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Text=" | " VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Text="CSV文件总数:" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalCsvFiles}" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：搜索结果分组列表 -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Background="#F5F5F5" Width="242" x:Name="LeftPanel">
                    <Border.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="True"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="2,1,2,1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TreeViewItem">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <ToggleButton x:Name="Expander" 
                                                          IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" 
                                                          ClickMode="Press"
                                                          Width="19" Height="19"
                                                          Margin="0,1,0,0">
                                                <ToggleButton.Style>
                                                    <Style TargetType="ToggleButton">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Border Background="Transparent" Width="19" Height="19">
                                                                        <Path x:Name="ExpandPath" 
                                                                              Data="M 0 4 L 8 4 L 4 0 Z" 
                                                                              Fill="#666666" 
                                                                              HorizontalAlignment="Center" 
                                                                              VerticalAlignment="Center"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsChecked" Value="True">
                                                                            <Setter TargetName="ExpandPath" Property="Data" Value="M 0 0 L 8 0 L 4 4 Z"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ToggleButton.Style>
                                            </ToggleButton>
                                            <ContentPresenter x:Name="PART_Header" 
                                                              Grid.Column="1" 
                                                              ContentSource="Header" 
                                                              HorizontalAlignment="Stretch" 
                                                              VerticalAlignment="Center"/>
                                            <ItemsPresenter x:Name="ItemsHost" 
                                                            Grid.Column="1" 
                                                            Grid.Row="1"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsExpanded" Value="False">
                                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                            <Trigger Property="HasItems" Value="False">
                                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#2196F3"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Border.Resources>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#2196F3" Padding="10">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="文件管理器" FontWeight="Bold" FontSize="13" Foreground="White"/>
                            </StackPanel>
                        </Border>

                        <TreeView Grid.Row="1" ItemsSource="{Binding SearchResultGroups}" BorderThickness="0" Background="Transparent" PreviewMouseLeftButtonDown="TreeView_PreviewMouseLeftButtonDown" Loaded="TreeView_Loaded">
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="{x:Type TreeViewItem}">
                                    <Setter Property="IsExpanded" Value="False"/>
                                </Style>
                            </TreeView.ItemContainerStyle>
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Results}">
                                    <Border Background="#E3F2FD" BorderBrush="#BBDEFB" BorderThickness="1" Margin="2,2,2,2" CornerRadius="3" Padding="6" Width="190">
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding GroupName}" FontWeight="Bold" FontSize="11" Foreground="#1976D2"/>
                                            <TextBlock Text="{Binding HeadersDisplay}" TextTrimming="CharacterEllipsis" 
                                                       FontSize="9" Foreground="#666" MaxWidth="180" Margin="0,1,0,0"/>
                                            <TextBlock Text="{Binding ResultCount, StringFormat='{}{0} 个文件'}" 
                                                       FontSize="8" Foreground="#999" Margin="0,1,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <HierarchicalDataTemplate.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Margin="2,1,2,1" CornerRadius="2" Padding="4" Width="175">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📄 " FontSize="10" Margin="0,0,3,0">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=IsSelected}" Value="True">
                                                                        <Setter Property="Foreground" Value="White"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding FileName}" FontSize="10" TextTrimming="CharacterEllipsis" MaxWidth="170" Foreground="#333">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=IsSelected}" Value="True">
                                                                        <Setter Property="Foreground" Value="White"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=IsSelected}" Value="True">
                                                                <Setter Property="Background" Value="#2196F3"/>
                                                                <Setter Property="BorderBrush" Value="#1976D2"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </DataTemplate>
                                    </HierarchicalDataTemplate.ItemTemplate>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </Grid>
                </Border>
                
                <Border Grid.Column="1" Background="#E0E0E0" BorderThickness="0,0,1,0" Width="20" x:Name="ToggleButton" MouseLeftButtonDown="ToggleButton_MouseLeftButtonDown">
                    <TextBlock Text="&lt;" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#666" FontWeight="Bold"/>
                </Border>
            </Grid>

            <!-- 右侧：数据展示 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>



                <!-- 数据表格 -->
                <Border Grid.Row="0" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="{Binding SelectedResult.FileName, StringFormat='文件: {0}', FallbackValue='请选择一个文件查看数据'}" 
                                       FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Content="打开文件" Command="{Binding OpenFileCommand}" 
                                    Margin="10,0,0,0" Padding="8,4" FontSize="11"
                                    IsEnabled="{Binding SelectedResult, Converter={StaticResource NullToBoolConverter}}"/>
                            <Button Content="打开路径" Command="{Binding OpenFileLocationCommand}" 
                                    Margin="5,0,0,0" Padding="8,4" FontSize="11"
                                    IsEnabled="{Binding SelectedResult, Converter={StaticResource NullToBoolConverter}}"/>
                            <Button Content="导出文件" Command="{Binding ExportFileCommand}" 
                                    Margin="5,0,0,0" Padding="8,4" FontSize="11"
                                    IsEnabled="{Binding SelectedResult, Converter={StaticResource NullToBoolConverter}}"/>
                        </StackPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding PaginatedData}" 
                                  AutoGenerateColumns="True"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  AlternatingRowBackground="#F5F5F5"
                                  GridLinesVisibility="All"
                                  BorderBrush="#CCCCCC"
                                  BorderThickness="1"
                                  RowHeaderWidth="0">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#007ACC"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource SearchKeywordConverter}">
                                                <Binding Path="."/>
                                                <Binding Path="DataContext.SearchKeyword" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#2196F3"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                        </DataGrid>

                        <!-- 分页控件 -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Content="首页" Command="{Binding FirstPageCommand}" Margin="2" Padding="8,4" FontSize="11"/>
                            <Button Content="上一页" Command="{Binding PreviousPageCommand}" Margin="2" Padding="8,4" FontSize="11"/>
                            <TextBlock Text="{Binding CurrentPage, StringFormat='第 {0} 页'}" Margin="10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalPages, StringFormat='共 {0} 页'}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <Button Content="下一页" Command="{Binding NextPageCommand}" Margin="2" Padding="8,4" FontSize="11"/>
                            <Button Content="末页" Command="{Binding LastPageCommand}" Margin="2" Padding="8,4" FontSize="11"/>
                            <TextBlock Text="每页显示:" Margin="10,0,5,0" VerticalAlignment="Center"/>
                            <ComboBox SelectedValue="{Binding PageSize}" SelectedValuePath="Tag" Width="60" VerticalAlignment="Center">
                                <ComboBoxItem Content="10" Tag="10"/>
                                <ComboBoxItem Content="20" Tag="20"/>
                                <ComboBoxItem Content="50" Tag="50"/>
                                <ComboBoxItem Content="100" Tag="100"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="3" Background="#F0F0F0">
            <StatusBarItem>
                <TextBlock Text="CSV数据分析工具 v1.0" FontSize="11"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
